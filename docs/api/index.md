# API Reference

Generated references:

- **Server (Python API)**: `./server/` via `just docs-api`
- **Server (REST/OpenAPI)**: `./rest.html` via `just docs-rest`
- **Client API**: coming later

Local docs dev: run `just` (defaults to `just docs-dev`).
Docs build for web: run `just docs-build`.
