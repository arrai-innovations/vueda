- **Convention-named action views in installed VUEDA packages**:
    - Applications that pass their `@` source alias through `vuedaViteConfig` can now load custom action views in development and production builds.
